import {imageName} from '{imagePath}';

const {figureName} = {{
    image: {imageName},
    rhythm: [
        {{ type: 'note', duration: 1 / 8 }},
        {{ type: 'note', duration: 1 / 8 }},
    ],
}};

export default {figureName};
